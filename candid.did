service : {
    canister_size: ()->(nat64) query;
    update_setting: ()->();
    size: ()->(text);

    page: ()->(bool) query;

    nano_time: ()->(nat64) query;
    data_add: ()->();
    data_get: ()->() query;
}